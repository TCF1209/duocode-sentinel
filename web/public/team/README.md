Drop two photos here, named to match `TEAM` in `web/components/pitch-view.tsx`:

    tang.jpg
    yee-teng.jpg

Square images, 256px or larger. Anything the browser cannot load falls back to
initials rather than a broken-image icon, so the page is presentable with or
without them and no code change is needed in between.
